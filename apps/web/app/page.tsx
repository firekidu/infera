export default function Home() {
  return (
    <main className="container">
      <h1>One‑Prompt App Builder</h1>
      <p>Describe the app you want, then we generate a live UI + API.</p>
      <a className="btn" href="/generate">Create from a prompt →</a>
    </main>
  );
}